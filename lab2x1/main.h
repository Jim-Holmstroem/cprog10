#ifndef MAIN_H
#define MAIN_H

    #include<iostream>
    #include<string>

    #include "date.h"
    #include "calendar.h"
    #include "gregorian.h"
    #include "julian.h"
    
    
    #define len(a) (sizeof(a)/sizeof(*a)) //used to get the number of elements in array

#endif
